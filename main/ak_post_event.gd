extends Node3D


# Called when the node enters the scene tree for the first time.
func _ready():
	#Wwise.register_game_obj(self, "meshInstance3D")
	#Wwise.post_event("play_x_debug_continuous_sound", self)
	pass

# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
	#pass
